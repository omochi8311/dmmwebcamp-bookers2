<%= render 'users/info', user: @user %>
<%= render 'books/newbook', books: @book %>

<h2>Users</h2>
<table class="table">
  <thead>
    <tr>
      <th>image</th>
      <th>name</th>
      <th colspan='3'></th>
    <tr>
  </thead>
</table>

<% @users.each do |user| %>
<div>
<p><%= user.name %></p>
<p><%= user.introduction %></p>
</div>
<% end %>