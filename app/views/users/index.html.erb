<%= render 'users/info', user: @user %>
<%= render 'books/newbook', books: @book %>

<h2>Users</h2>
<table class="table">
  <thead>
    <tr>
      <th>image</th>
      <th>name</th>
      <th colspan='3'></th>
    <tr>
  </thead>
</table>
<tbody>
 <tr>
   <td>
    <table class="table table-striped">
    <% @users.each do |user| %>
    </table>
   </td>
   <td><%= image_tag current_user.get_profile_image %></td>
   <td><%= user.name %></td>
   <td><%= link_to "show", user_path(user.id) %></td>
  </tr>
</tbody>
<div>

</div>

<% end %>